{
  "name": "jpena-astro",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && aws s3 sync dist s3://jpena-portfolio-hugo && npm run invalidate",
    "invalidate": "aws cloudfront create-invalidation --distribution-id E1PM39SMSCCPKG --paths '/*' --no-cli-pager"
  },
  "dependencies": {
    "astro": "^4.16.4"
  }
}
