## Run (Astro)
`cd astro`
`npm install`
`npm run dev`

## Build
`cd astro`
`npm run build`

## Deploy (S3)
`cd astro`
`npm run deploy`

Requires AWS credentials available locally (AWS CLI profile or env vars).
